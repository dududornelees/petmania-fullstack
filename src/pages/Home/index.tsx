import { MainTemplate } from "components";

import * as G from "styles";

export const Home = () => {
    return (
        <MainTemplate>
            <G.Container>Home!</G.Container>
        </MainTemplate>
    );
};
